{
  "environments": {
    "test": {
      "env": {
        "DATABASE_URL": {
          "required": true
        }
      },
      "scripts": {
        "test": "mocha --exit ./tests/test && vue-cli-service test:unit"
      }
    }
  }
}
